# Egle Motors — Staff Training Portal

A single-page staff guide to the Egle Motors applications: what each app is
for, who uses it, and the steps for everyday tasks.

Static site. `index.html` is self-contained — no build, no dependencies.
Published with GitHub Pages.
